generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model links {
  id          Int     @id @default(autoincrement())
  link        String
  description String?
}
